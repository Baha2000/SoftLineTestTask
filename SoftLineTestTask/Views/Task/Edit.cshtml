@model SoftLineTestTask.Models.TaskViewModel

@{
    ViewBag.Title = "Редактирование задачи";
}

<h2>Редактирование задачи</h2>

@using (Html.BeginForm("Edit", "Task", FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.HiddenFor(task => Model.Task.Id)
    <div class="form-group">
        <label for="name" class="col-sm-2 control-label">Название:</label>
        <div class="col-sm-10">
            @Html.TextBoxFor(task => Model.Task.Name, new { @class = "form-control", @required = "required"})
        </div>
    </div>
    <div class="form-group">
        <label for="description" class="col-sm-2 control-label">Описание:</label>
        <div class="col-sm-10">
            @Html.TextAreaFor(task => Model.Task.Description, new { @class = "form-control", @required = "required"})
        </div>
    </div>
    <div class="form-group">
        <label for="statusId" class="col-sm-2 control-label">Статус:</label>
        <div class="col-sm-10">
            @Html.DropDownListFor(task => task.Task.StatusId, new SelectList(Model.Statuses, "Id", "Name", Model.Task.StatusId), new { @class = "form-control", @required = "required" })
        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <input type="submit" value="Сохранить" class="btn btn-primary">
        </div>
    </div>
}