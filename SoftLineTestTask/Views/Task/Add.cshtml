@model SoftLineTestTask.Models.TaskViewModel

@{
    ViewBag.Title = "Создание задачи";
}

<h2>Создание задачи</h2>

@using (Html.BeginForm("Add", "Task", FormMethod.Post, new { @class = "form-horizontal" }))
{
    <div class="form-group">
        <label for="name" class="col-sm-2 control-label">Название:</label>
        <div class="col-sm-10">
            @Html.TextBoxFor(task => Model.Task.Name, new { @class = "form-control", @required = "required"})
        </div>
    </div>
    <div class="form-group">
        <label for="description" class="col-sm-2 control-label">Описание:</label>
        <div class="col-sm-10">
            @Html.TextAreaFor(task => Model.Task.Description, new { @class = "form-control", @required = "required"})
        </div>
    </div>
    <div class="form-group">
        <label for="statusId" class="col-sm-2 control-label">Статус:</label>
        <div class="col-sm-10">
            @Html.DropDownListFor(task => task.Task.StatusId, new SelectList(Model.Statuses, "Id", "Name"), new { @class = "form-control", @required = "required" })
        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <input type="submit" value="Создать" class="btn btn-primary">
        </div>
    </div>
}